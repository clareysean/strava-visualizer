import React from "react";

type Props = {};

export default function Home({}: Props) {
  return <div>home</div>;
}

// call an api method

// POST https:

//www.strava.com/oauth/token with its client ID and client secret to exchange the authorization code for a refresh token and short-lived access token.
